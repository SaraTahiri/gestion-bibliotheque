/*package model;

public class Test {
    public static void main(String[] args) {
        UtilisateurModel model = new UtilisateurModel("users.csv");
        
        try {
            // Ajout d'utilisateurs
            model.ajouterUtilisateur(new Utilisateur("Meryem", "newPass123", "Admin"));
            model.ajouterUtilisateur(new Utilisateur("Sara", "password", "Admin"));
            model.ajouterUtilisateur(new Utilisateur("Salma", "admin123", "Admin"));
            model.ajouterUtilisateur(new Utilisateur("Bob", "password", "Membre")); // Duplication volontaire

           
            
            model.sauvegarderCSV();

            // Affiche la liste nettoyée
            model.listerUtilisateurs();
        } catch (Exception e) {
            System.err.println("Erreur : " + e.getMessage());
        }
    }
}
*/
